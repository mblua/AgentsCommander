# #1937 — Coding Agent + requested Profile selection locks

Status: READY_FOR_IMPLEMENTATION — architect candidate for frozen-digest review, not permission to skip owning-dev/Grinch consensus or coordinator implementation gate. Product code unchanged. Full / design-bearing. Canonical issue https://github.com/mblua/AgentsCommander/issues/1937. Base7aef1d14e6bb0255b1430614c078145335dd54a4, branch feature/1937-selection-locks. Current source HEAD/local origin/main match; no source/index/ordinary-untracked drift observed. Coordinator owns next bounded fetch and semantic drift classification.

Threat model: routine application feature with concrete app/CLI lost-update, stale-force, invalid-state and late-restart hazards. OS sidecar exclusion and operation lifetime ownership are required; release/signing, hostile-host/executable attestation and unrelated hardening are not. Local environment differs from CI Node22 (local24); exact-head platform CI owns parity. No live input/capture tests or install authorized/performed.

## Settled scope and design

Approved v3 SHA2567901780e9d936c603ba0a704b9cf002197a8bdf5cf687b795b2c0fe37ae7f7b0, shared .ac/plans/issue_1937. User decisions and ISSUE take precedence over historical proposals; archived technical PLAN was not frozen. User separately approved dedicated Save default on2026-09-11 via coordinator message021900: save selected provider/requested letter + Start locked; never automatic save or mutate existing replicas. Coordinator confirmed no new creation form/CLI flags: individual assignment/unlock after creation plus preserved local state on retry supplies per-replica exceptions.

1. Protect persisted Coding Agent ID + requested Profile letter. Preserve resolver/content drift/fallback; do not freeze commands/model/environment/version. Add tooling.selectionLocked boolean, absent false, invalid shape/read/flag explicit invalid. True requires complete valid pair. Ordinary bulk skips locked writes and restarts; deliberate individual assignment preserves lock.
2. Six mutually exclusive Apply to/+lock radios, three existing scopes. Canonical origin Matrix identity defines Kind across configured roots; room scope is one room. Conflicting bulk +lock requires Cancel, Apply only to unlocked, Force all including locked (English, Cancel left). Equal protected pairs still conflict. Force only bound reviewed candidate snapshot and decision, never invalid state or untrusted targets.
3. Independent upper-bar unlock scope changes flag only, no pair/default/restart. Focus-unlocked allows other protected candidates; zero disables Nothing to remove. Complete3-of4 example shows actual removed count,0of4protected and preserved outside/default/pairs; partial/unknown states cannot claim full success.
4. Compact Kind list renders locked rows only but retains full target membership/counts/eligibility. Room sidebar live/inactive KEEP chip uses persisted state; root/origin surfaces unaffected. Saved pair bar/default display never substitutes unsaved picker selection.
5. Matrix tooling.replicaSelectionDefault contains complete pair+flag; copy once when config absent under sidecar. Existing config skips reading/applying default, preserves local exceptions/context/unknown keys, and invalid existing bytes fail visibly. GUI and CLI creation use lossless shared initialization. Membership/repository/config work remains partially committable; truthful errors and retry preservation replace global rollback promises.
6. Stable per-file sidecar in local_config_io serializes read-through-publish across cooperating processes,5s/50ms, canonical parent, never truncate/delete. Narrow gitignore patterns cover lock runtime files. All relevant writers preserve protective state; strict selection writes compare expected identity/pair/flag inside callback. Existing atomic publish remains.
7. Shared turn in session/selection; check worker reentry BEFORE spawn, acquire owned guard inside backend task. Desktop/web wrapper mutation+event publication survives dropped caller; inner operations do not reacquire. Keep through actual coordinator reply; no cancellation by timeout/drop. Self-switch guard covers adjacent persist/restart, excludes idle settle. Coordinator worker never waits for turn.
8. SHA256 typed tuple fingerprints bind operation,anchor/scope,pair,mode/decision,membership,target state and relevant restart sessions. Full revalidation before writes; per-file CAS for later races. Separate typed removal/default operations; default has own CAS fingerprint. Existing fields/scope literals retained. New backend fields are always explicit; older producer absence is frontend unknown/unsupported.
9. Narrow WebSocket mutation-command timer exemption preserves promise pending beyond30s while connected; other commands retain timer/disconnect behavior. Selection previews/default reads can time out behind another operation; discard late replies and recover through existing refresh/reopen with a fresh read, never reuse timed-out authority or replay mutation. #1941/#1942 specify barrier/fake-timer tests; no new retry UI or exemption. Disconnect means outcome unknown, refresh after reconnect, never replay.

## Evidence and architecture gate

Graph-first project identity confirmed authorized room12 root,23036nodes/152508edges,ready. Cited source metadata_changed required direct source verification. Scripts/plan/frontend tests excluded by design and inspected directly. Bounded specialist reports in room-shared/issue-1937/rust-evidence.md, ui-evidence.md and probe-os-lock.md; architect inspected decisive code and corrected pre-spawn task-local check. No peer-private filesystem reads.

Writer map: coding_agent_profiles pair/profile/hash/default writers; agent_config historical metadata; ac_discovery context updates; entity_creation whole-object creation and lossless repo-sync; replica_identity repair; root_agent merge; project_settings writer share local_config_io JSON mutation. Eleven call sites in specialist inventory include the out-of-scope project-settings file. Two destructive replica paths (create_or_update_replica_on_disk, GUI create_workgroup) must become lossless. CLI team add-member calls creation even when membership exists; no CLI source change needed, integration test proves retry. Nonselection writers must never reset malformed replica tooling.

Baseline validation: fmt check exit0, typecheck exit0,4focused frontend files/54tests pass; no full Rust build/feature tests claimed. Windows std-lock feasibility probe demonstrates exclusion/release/aliases, not complete cross-platform production proof. delivery-evidence.md records commands/logs/tool versions/workflow/rules. All post-feature tests, failure interleavings and exact-head CI are specified owner/time gates in self-contained phases, not falsely reported as already passed.

Clean-base cycle measurement:199modules,1091arcs,3934sites,114SCCs,cyclicSccs1. Existing cycle40d22fc71179d71f has86members. Regenerated86581-byte record is byte-identical to committed src-tauri/module-arcs.txt. Single new planned arc: commands::entity_creation -> config::coding_agent_profiles, initializer anchors entity_creation.rs:1223/2923, inside same existing SCC. No removed arcs. std-only local_config_io and unchanged cli::team add no dependencies across clean SCC boundaries. Turn/transport/discovery calls reuse measured arcs; lib.rs macro-only commands::config registrations are an explicitly unmeasured detector blind spot, not evidence of a baseline lib->commands/config arc. #1941 stays macro-only and requires scoped re-review if that arc appears in post measurement. Projection computes1092arcs/1cyclicScc and identical complete SCC member sets; zero new cross-boundary arcs. No lower persistence layer gains AppHandle/Tauri/commands dependency. Projection is planning proof only; actual clean final source graph, member-set equality, record byte equality and structural guards are mandatory at each implementation review. Runtime ordering proof is separate.

## Delivery gates and remaining execution evidence

| Gate | Source / expected result | Owner and time / failure behavior |
|---|---|---|
| CI parity | Actual base workflows +13 required contexts from room-shared/1937-main-rules.json, API2026-09-11T02:08Z. All triggered +required jobs green at exact PR head; conditional skips explained by actual filters | Coordinator refreshes rules/diff at each landing; shipper waits for exact-head checks. Any unexplained failure blocks merge |
| Toolchain/build | Node22/npm11.6.2/stable Rust in CI; locked dependency resolution; existing target/dist roots; record local version/env | Implementer before checks. Unexpected resolution/lockfile diff stops phase; no toolchain upgrades hidden in feature |
| Git authority | Open native child, synchronized issue-numbered feature branch, clean affected paths/index/status | Coordinator before each phase and PR update; unrelated target drift does not restart design; relevant drift refreshes smallest evidence |
| Process/config/cwd | Authorized repo cwd, relevant inherited BUILD_PROFILE/RUSTFLAGS/CARGO_TARGET_DIR recorded; logs in room-shared; no secret dump | Implementer pre-run; invalid/mismatched cwd/artifact root blocks execution |
| Scope/validation | Exact phase inventory, expected code/test shape, named positive tests; final staged/tracked/untracked/config/lockfile review | Implementer/reviewer before PR. Out-of-scope diff requires scoped plan revision and new digest vote |
| Mutation/recovery | Recheck own paths before writes, scoped originals/output identity; restore only own unaltered output, preserve external edits | Implementer each write/failure. Never broad reset/restore or runtime bulk rollback; report actual partial config/restart subset |
| Bounded diagnostics | Local shell timeouts600s focused/1800s check-clippy, existing CI limits, retained logs/exits. Backend lifecycle waits for real settlement, never drops accepted restart receiver to fake cancellation | Implementer validation; runtime owner reports true partial failure. Timed-out/cancelled commands fail proof |
| Evidence | Baseline results separately marked from planned feature/platform tests and source projection | Architect readiness; owning dev/reviewer post-implementation. Missing feasibility/unspecified owner blocks readiness; unexecuted future feature tests block landing, not code creation |

Platform test change is explicit in #1938 workflow inventory because existing Linux/macOS jobs do not automatically execute new Unix tests. Current workflow paths activate all regression jobs for PRs, plus feature-push branch validation. Thirteen configured contexts include terminal-snapshot-portable four OS variants; Linux release parity and #1850 hosted-profile jobs are triggered too. Version/bundle checks only when configured paths require them. Existing #480 allowance only exact committed classifier; no new debt accepted.

Enhanced controls not applicable: executable self-hash/host attestation, DLL/SDK manifests, adversarial parent-PATH quarantine, broad mutation ledgers and custom runner supervision. Applicable concurrency controls are confined to config sidecars/selection owner because actual race hazards exist. Older binaries/external editors ignoring protocol and unsupported filesystem locking are outside guarantee (fail lock errors visibly); no globally atomic multi-replica transaction. Serialization may wait behind long restarts; UI stays pending while connected, with independent disconnection diagnostics.

## Partition, native IDs and shared-file handoffs

PARTITION: 6 phases. Trigger: multiple owners/contracts and27 unique product paths; no mechanical exception. Every phase is self-contained, one owner, one external contract at most, <=10 modified files, no added/deleted product files. Each requires landed predecessor/current-main branch; all parallel-with none. Frontend client phase class is design-bearing because it adjusts operation completion timing.

Native children #1938-#1943 were created OPEN by coordinator and mapped2026-09-11T02:20Z. Branch slugs exactly match filenames. No placeholder issue IDs. Real-ID phase documents inline full decisions, commands, delivery contract and wire schema where consumed. No implementer needs epic for an action.

Shared edits: ac_discovery passes #1938 -> #1941; commands/config and web/commands pass #1940 -> #1941. Every other product file has one phase owner. No shared-file parallelism. #1938 guard safe with old behavior; #1939 lossless storage without new control; #1940 fixes ordering; #1941 additive API compatible with old client; #1942 wrappers/completion behavior; #1943 complete approved UI+badges. Main must be green/shippable at each landing.

Phase table follows with SHA256 of exact bytes (uppercase). Epic digest is computed after this table; never embed its own digest. Any changed phase bytes invalidate that phase and epic votes. Coordinator updates native child issue bodies from final text, verifies parent-child linkage after consensus, purges before each phase implementation and obtains matching owning-dev/Grinch PHASE_EXECUTABLE votes. Final merged-main build after last phase, no install.

| Child | Class | Owner | Files | Depends-on | Parallel-with | Phase-SHA256 |
|---|---|---|---:|---|---|---|
| [#1938](1938-config-write-locks.md) | design-bearing | ac-dev-rust-v4 | 3 | none | none | E9147B778F1B3DAD275153984893473D2AA7694334C181680EBB8F7A1DFA964B |
| [#1939](1939-replica-selection-state.md) | design-bearing | ac-dev-rust-v4 | 6 | #1938 | none | 51A50421B614ABF4DA291F3B84CE20734A9558D727F80C90AAD9DE5458C3CE1E |
| [#1940](1940-selection-operation-lifetime.md) | design-bearing | ac-dev-rust-v4 | 4 | #1939 | none | 54704707D5E8D204746F718955F429C628EF8466D989D124ACC17F50D35179F7 |
| [#1941](1941-selection-lock-api.md) | design-bearing | ac-dev-rust-v4 | 4 | #1940 | none | F93345EB8F31347F7FE7457057D7906EC8CAA096D60528D77A94D742086283D2 |
| [#1942](1942-selection-lock-client.md) | design-bearing | ac-dev-webpage-ui-v4 | 5 | #1941 | none | A8697CC9B26467525DE924A7A393015216768048E017BCDD249053E5E153EB24 |
| [#1943](1943-selection-lock-picker.md) | design-bearing | ac-dev-webpage-ui-v4 | 8 | #1942 | none | D8A07A83D5A52A5508F55031A42E8F98D77CA6A4C00E8B37DD11CFD70D00A786 |
