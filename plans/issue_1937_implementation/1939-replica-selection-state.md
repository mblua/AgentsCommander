# #1939 — preserve replica selections and materialize creation defaults

Status: architect candidate for review. Child #1939. Parent #1937. Class design-bearing. Owner ac-dev-rust-v4. Depends on landed #1938. Contract persistence only. Five modified files, zero added/deleted:
- src-tauri/src/config/coding_agent_profiles.rs
- src-tauri/src/config/agent_config.rs
- src-tauri/src/commands/entity_creation.rs
- src-tauri/tests/cli_workgroup_team.rs
- src-tauri/module-arcs.txt

## Stored contract

Replica top-level config.json gains optional `tooling.selectionLocked:boolean`; absent in otherwise valid config means false. Protect currentCodingAgent plus requested `profile` letter, not resolved command/environment/model/content. Strict read uses existing replica_identity::read_wg_replica_config_read_only (duplicate keys/object/identity validation), then validates tooling object/flag and saved pair. Invalid JSON, tooling shape, flag type, or true with incomplete/invalid pair is Invalid diagnostic, never Unlocked. Missing pair on legacy unlocked config remains valid and may be deliberately assigned. Stored profile prefers profile over legacy instanceProfileOverride; preserve existing divergence warning. Requested/effective fallback remains resolver-owned.

Matrix top-level tooling.replicaSelectionDefault = {codingAgentId:string,requestedProfile:A-Z,selectionLocked:boolean}. Absent preserves old creation behavior. Invalid object/fields/flag fails reading/creation; provider resolution diagnostics remain visible. Read complete default from one atomic JSON snapshot. Matrix profile-only defaultProfile remains independent. Default changes do not update existing replica selections.

Add pure parsing/types and typed functions in coding_agent_profiles:
- read_replica_selection_state returns saved pair + unlocked/locked/invalid diagnostic.
- write_replica_selection takes validated settings/path/pair plus SelectionWriteIntent and expected state (canonical identity,pair,flag). Intent: Individual, IndividualAssignLock, BulkOrdinary, BulkAssignLockUnlockedOnly, BulkForceReviewed. No free bool allows caller to bypass policy.
- clear_replica_selection_lock checks expected state and writes only flag=false; already false returns AlreadyUnlocked without publishing. Keep the #1938 funnel signature: inside coding_agent_profiles use a call-local typed outcome marker, initially unset. After strict identity/pair/flag validation and expected-state comparison INSIDE the guarded callback, an already-false/absent flag sets AlreadyUnlocked and immediately returns a private callback Err before any object mutation. The existing mutate(obj)? exits before serialization/temp creation/publication. Outside the funnel translate only Err plus that locally set marker into successful WriteOutcome {changed:false,published:false}; never recognize it by error-string matching. Every lock/read/parse/CAS/write error without that marker propagates unchanged. A true flag follows normal flag=false publication. No pre-guard shortcut, new funnel API, extra file, or module arc.
- read_replica_selection_default and write_replica_selection_default validate complete default and optional expected prior default for CAS.
All mutations reread strict config and expected state INSIDE update_config_json_object closure under #1938 sidecar. Strict reread is read-only, never recursively takes mutation guard. If mismatch return stale error before publishing; no stale overwrite. False->true lock transition and actual publication separately represented in WriteOutcome. Invalid state cannot be force-repaired implicitly.

Assignment writes currentCodingAgent, profile, instanceProfileOverride (matching profile), instanceProfileOverrideSource=manual in one publish; assign-lock adds true; ordinary deliberate edits preserve existing flag. Preserve unknown keys, lastCodingAgent, profileContentHash. set_replica_coding_agent_selection becomes deliberate-preserving wrapper. write_profile_to_launch_path permits a new valid requested letter while preserving Coding Agent/flag; reject null clear for protected pair. write_tooling_string (including content-hash update) must not reset malformed replica tooling. In agent_config::upsert_config the discriminator is JSON shape, not path or replica detection: for BOTH root/config.json and root/<agent_local_dir>/config.json, absent top-level tooling creates an object, an object is preserved/updated, and any present non-object (including null) returns an error before mutation/publication. This deliberately stops historical top-level repair for plain-repo configs too; compatibility means valid/missing tooling and nested codingAgents repair remain supported, not malformed top-level replacement. Keep ensure_object for nested codingAgents; do not globally change it or unrelated per-instance timestamp writers. Preserve malformed selectionLocked values untouched on these metadata writes when tooling itself is an object. No dependency from agent_config to coding_agent_profiles is needed: validate local shape within existing callback.

## Creation and retry

create_or_update_replica_on_disk replaces whole-object write with guarded lossless update patterned on sync_workgroup_repos_inner: repair_wg_replica_config_value, normalize_wg_replica_context_entries using EXISTING context, update required identity/repos, preserve tooling/custom context/unknown keys. Config absence inside guard is first-creation boundary; directory existence alone is not. Inside the replica sidecar callback use try_exists to distinguish absent from errors. Only for absent config read Matrix default as one atomic JSON snapshot (read-only, no second sidecar), validate and materialize. Existing config skips default reading entirely, so a later invalid Matrix default cannot block a valid local retry. Concurrent creator winner becomes authoritative; retry merges existing bytes and never reapplies default.

GUI create_workgroup replica loop must call the same guarded initialization helper or identical shared local helper; remove destructive replacement at former line2923. CLI create_workgroup_on_disk and team add-member already reach create_or_update_replica_on_disk. No new CLI flags/form/creation exception DTO. Coordinator confirmed individual assignment/unlock after creation and preserving existing local exceptions meets user requirement. Recreated missing config uses current default; reopen/archive/restore retains existing state. A malformed existing config must remain byte-identical and fail, never be treated as absent. Empty interrupted directory may initialize.

Membership/repo/config publication are not a global transaction. Existing CLI add-member invokes replica creation even when membership already exists; keep retry ability and report partial creation errors accurately. Do not roll back unrelated team membership or repos. A bad default must fail before publishing a misleading replica config; any directory/previous member successes reported honestly.

## Proof and preserve list

Add issue_1937_selection_state tests in coding_agent_profiles for absent/invalid/duplicate JSON, protected incomplete pair, coherent dual fields, fallback letter content changes, expected-state CAS, skip/force/individual intent table, null protected-profile rejection, unknown keys/history/hash. Add issue_1937_creation tests in entity_creation for default/no-default, existing config override, concurrent/retry initialization, invalid default, context merge, GUI/on-disk parity. Extend existing cli_workgroup_team integration test with issue_1937_repeat_member_preserves_config: repeated add preserves tooling/pair/flag/custom context/unknown keys; concurrent guarded config write survives; no live GUI required.

Add issue_1937_selection_state_already_unlocked_no_publish: false and absent flags retain exact deliberately non-pretty JSON bytes, changed=false/published=false, and a pre-existing directory at the known temp-config path remains untouched (positive true->false control reaches that obstruction and fails temp creation). A false expected state racing to true must return stale, not AlreadyUnlocked; malformed state, lock timeout and IO errors cannot become success. Add issue_1937_selection_state_tooling_shape tests in agent_config for BOTH target paths: missing/object succeed, null/string/array/number/bool fail with exact bytes preserved, nested malformed codingAgents still repairs, malformed selectionLocked inside valid tooling remains untouched, and valid plain-repo metadata remains compatible. These tests run under the existing issue_1937_selection_state filter.

From src-tauri: `cargo test --locked --lib issue_1937_selection_state`; `cargo test --locked --lib issue_1937_creation`; `cargo test --locked --test cli_workgroup_team issue_1937_repeat_member_preserves_config`; `cargo check --locked --all-targets`; `cargo clippy --locked --workspace --all-targets -- -D warnings`; `cargo fmt --all -- --check`. Named new tests must have positive count. No app input capture.

Only new project arc: commands::entity_creation -> config::coding_agent_profiles at create_or_update_replica_on_disk/GUI shared initializer (base anchors1223/2923). Both in baseline SCC40d22fc71179d71f (86 members). All other calls reuse existing arcs; do not add clean-boundary arcs. Regenerate module-arcs.txt from clean candidate graph and commit its single new arc; require generated bytes equal record, cyclicSccs=1 and exact same members. Run loops_layering, instance_gitignore_layering, project_settings_layering integration tests. No persistence layer gains transport/AppHandle dependency.

Green boundary: additive storage/internal initialization works with old APIs; no lock/default controls exposed yet. Existing configurations behave as before except lossless retry and explicit malformed-state failure. #1940 owns later commands/config edits; #1941 owns discovery/API exposure.

## Per-phase delivery contract (binding)

Planning reference base 7aef1d14e6bb0255b1430614c078145335dd54a4, original issue branch feature/1937-selection-locks. This phase implements only after listed earlier phases have landed. Coordinator pins synchronized current main as actual phase base and creates `feature/1939-replica-selection-state` in authorized repo-AgentsCommander; record exact SHA/branch/index/tracked/untracked before edits. Never assume a predecessor branch is landed. Coordinator classifies fetched drift before first edit and before PR create/update; refresh only semantically affected source/toolchain/format/workflow evidence, not unrelated movement. Open real child issue, matching frozen digest votes, and coordinator implementation gate are preconditions.

Threat model routine application feature. Concurrency/persistence/lifecycle controls are task-required; executable/DLL attestation, hostile-host hardening, release signing, and arbitrary install changes are not applicable. Never edit another agent memory/root or task state. Work from verified authorized repo cwd; Bash through configured Windows Git Bash as session requires. Keep build outputs in repository ignored target/dist/node_modules, diagnostics in room-shared/issue-1937. Record meaningful versions and relevant inherited CARGO_TARGET_DIR/RUSTFLAGS/BUILD_PROFILE; no credential dumps. CI uses Node22/npm11.6.2/stable Rust, local observed Node24/npm11.6.2/Rust1.97.1. Use npm ci/cargo --locked when resolving; do not change lockfiles/toolchain to suppress failures.

Immediately before mutation compare selected paths/head/index with frozen record. Preserve scoped originals. On failure restore only this run output still equal to recorded written bytes; preserve external edits and report conflict. No broad reset/restore/cleanup. End with `git diff --check`, `git diff --name-only`, `git diff --cached --name-only`, `git status --short --untracked-files=normal` and explicit lockfile/config/workflow diff. Unexpected scope fails; update plan/review before continuing. Failed, timed-out, cancelled or zero-selected tests never pass. Use shell timeout (local focused tests600s, check/clippy1800s; stop and report timeout) and retain stdout/stderr/exit. No live session/screenshot/input tests without user authorization.

Rust phases: on clean actual phase base and clean final candidate, from repo root run the exact instrument with distinct base/post artifact names:
```bash
node "../repo-personal/ObsidianVault/Coding Agents/IA-Programming/rust/01-rust_module-dependency-cycles.mjs" src-tauri --emit-graph "../room-shared/issue-1937/phase-post.json" --json > "../room-shared/issue-1937/phase-post-report.json"
node "../repo-personal/ObsidianVault/Coding Agents/IA-Programming/rust/Levelization/02-levelize.mjs" rank "../room-shared/issue-1937/phase-post.json" > "../room-shared/issue-1937/phase-post-levels.json"
node scripts/02-module-arc-record.mjs --graph "../room-shared/issue-1937/phase-post.json" --out "../room-shared/issue-1937/phase-post-arcs.txt"
cmp src-tauri/module-arcs.txt "../room-shared/issue-1937/phase-post-arcs.txt"
```
Use child-number-specific artifact subdirectory for each phase; never overwrite prior baseline. Detector exit1 with valid graph means existing cycle; exit2/3 fails measurement. Compare cyclicSccs and exact sorted SCC member sets; compare unique from/to arc differences (not findings); each new arc must be inside a pre-existing cyclic SCC. #1939 alone permits entity_creation->coding_agent_profiles; all other phases zero added/removed Rust arcs. Require arc-record bytes exactly equal generated final graph and layering tests green: from src-tauri `cargo test --locked --test loops_layering --test instance_gitignore_layering --test project_settings_layering`. Actual post code measurement belongs implementation/reviewer gate; a planning projection is not post proof.

Coordinator/shipper refresh configured rules and require all triggered jobs plus13 configured checks on exact PR head: validate-branch-name, lockfile-drift, rust-regression, frontend-regression, rust-regression-linux, rust-regression-macos, terminal-snapshot-portable (windows-latest/ubuntu-latest/macos-15/macos-15-intel), windows-release-cli-smoke, test-debt, rust-fmt. Regression workflow applies every PR; additional Linux release parity and #1850 platform jobs also must pass. Conditional version/bundle jobs apply if their configured paths change; inspect actual final diff. Existing #480 frontend allowance only exact committed classifier result. Any unexplained red/skipped required check blocks merge; no bypass/direct main push. Final merged-main build after last phase, no install. Child acceptance includes behavior tests plus these delivery conditions, not just compilation.
